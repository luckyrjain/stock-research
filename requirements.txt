# Version bounds cap each dependency below the next major release beyond
# what's currently verified working (see tests/), so a fresh `pip install`
# can't silently drift onto an untested breaking release. Floors are
# unchanged from what the code was originally built against. There is no
# lockfile — bumping a floor or cap here should be paired with a full
# `pip install -r requirements.txt && python -m pytest tests/` run.
crewai[anthropic]>=0.80.0,<2.0.0
litellm>=1.60.0,<2.0.0
pandas>=2.0.0,<4.0.0
numpy>=1.24.0,<3.0.0
psycopg2-binary>=2.9.0,<3.0.0
sqlalchemy>=2.0.0,<3.0.0
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
yfinance>=0.2.36,<2.0.0
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.0.0,<7.0.0
gnews>=0.3.7,<1.0.0
feedparser>=6.0.0,<7.0.0
python-dotenv>=1.0.0,<2.0.0
rapidfuzz>=3.0.0,<4.0.0
redis>=5.0.0,<6.0.0
sentry-sdk>=2.0.0,<3.0.0

# Optional providers (install only what you need)
groq>=0.8.0,<2.0.0
google-generativeai>=0.8.0,<1.0.0

# Dev / test
pytest>=8.0.0,<10.0.0
httpx>=0.27.0,<1.0.0  # required by fastapi.testclient.TestClient (tests/test_api.py)
