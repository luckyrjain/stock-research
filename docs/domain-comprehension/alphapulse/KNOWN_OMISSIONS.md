# Known Omissions

<!-- Deliberate scope limits — NOT unanswered questions (those go in UNKNOWNS.md). -->

| Category | Item | Reason excluded | Impact on comprehension |
|----------|------|-----------------|-------------------------|
| MCP gap | GitLab squad mapping (Session 0b) | No GitLab/Jira integration configured; this is a single-operator project (see `docs/PRD.md` §17.1 bus-factor-of-one) with no team structure. | None — ownership is trivially "sole operator" for every path; recorded in `SQUAD_MAP.md`. |
| MCP gap | Datadog runtime validation (P2b) | `plugin:datadog:mcp` is configured but failed to connect (`ENOTFOUND`) in this session; no KubeSense MCP is configured at all. | P2b produces no runtime-confirmed edges. All flow/dependency claims in `ALPHAPULSE_MAP.md`, `BUSINESS_FLOWS.md`, and `DEPENDENCY_GRAPH.yaml` are capped at code-derived confidence (max HIGH via static evidence, never "runtime_confirmed"); `manifest.yaml runtime_validation.skipped: true`. |
| Tooling gap | Mechanical knowledge-graph pass (P0.5) | The `understand-anything` plugin skill is not installed in this environment (`find ~/Library/Application Support/Claude -iname SKILL.md \| grep understand` → no match). Fallback tools `madge`, `dependency-cruiser`, `pydeps` are also not installed (`jdeps` is present but irrelevant — no Java code in this repo). | P0.5 "Mechanical Insights" is produced via manual `rg`/`find`/`wc -l` discovery instead of a generated call/complexity graph. Top-complexity-file and fan-in rankings are approximated from line counts and grep-based caller counts, not a real static-analysis tool; disclosed inline in `ALPHAPULSE_MAP.md` § Mechanical Insights. Code/graph divergence classification in P2 is correspondingly evidence-thin (no mechanical graph to diff against) — classified from manual reading only. |
| Excluded repository | None — single-repo layout | This engagement's `workspace_layout` is `single-repo`; there is exactly one `.git` at the workspace root. `backend/` and `frontend/` are two stacks within it, both analyzed. | None. |
| Scope decision | `frontend/e2e/*` Playwright specs treated as corroborating test-posture evidence only, not as behavioral source of truth | Per `frontend/CLAUDE.md`, every backend response in these specs is mocked — they prove UI behavior under a scripted backend, not real backend behavior. | Cited only in P4 Quality & Ops test-coverage discussion, never as evidence for API/data-ownership/flow claims. |
| Scope decision | `backend/output/` cache contents not enumerated | Per root `CLAUDE.md`, this directory is gitignored, cache-only, and fully regenerable; nothing durable lives there. | Cache *shape*/TTL contract is documented from `backend/core/cache.py` source, not from inspecting actual cached files (none may exist in a fresh checkout). |
