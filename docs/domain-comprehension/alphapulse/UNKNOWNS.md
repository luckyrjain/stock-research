# Unknowns Register

| Area | Question | Why code is insufficient | Likely owner | Blocking impact |
|------|----------|---------------------------|--------------|------------------|
| Schema documentation | Is the table count 22 or 23? | Root `CLAUDE.md`'s summary table says 22; `backend/CLAUDE.md`, `docs/database.md`, and `backend/db/models.py` itself all independently agree on 23 (confirmed by two separate research agents counting `Table(...)` definitions). Trusting the code: **23 is correct**; root `CLAUDE.md` is stale. | Sole operator | None on comprehension (resolved by evidence precedence); cosmetic doc fix only |
| Doc drift | Is `tools/securities_master.py::resolve_symbol()` "not yet wired into anything" (as `backend/CLAUDE.md`'s own "Securities master + symbol resolver" section states) or actually wired into `csv_import.py`/`hdfc_sync.py` (as `backend/CLAUDE.md`'s own later "Broker CSV/XLSX import" and "HDFC Securities" sections, and direct grep, confirm)? | Two sections of the same doc disagree with each other and with the code; the quality/ops agent directly confirmed live call sites (`csv_import.py:25,219`, `hdfc_sync.py:71,294`) | Sole operator | None on comprehension; the resolver *is* wired in per code — the doc section is simply stale |
| `api.py` internal logic | Full internal logic of routes at `api.py` lines ~1240-2712 (roughly half the file) — `/api/prices/history`, `/api/peers`, `/api/financials`, `/api/shareholding-detail`, `/api/insider-activity`, `/api/street-consensus`, `/api/verdict-history`, `/api/sme-signals*`, `/api/screener*`, `/api/consolidated`, `/api/auth/*`, `/api/api-keys*`, `/api/v1/consolidated` beyond what `docs/api-reference.md`/`backend/CLAUDE.md` document | The backend-core research agent read `api.py` lines 1-1239 in full and grepped route signatures for the rest, prioritizing full coverage of `main.py`/`auth.py`/`core/`/`db/`/`analyst/`/`analytics/`/`config/` within its discovery budget | Sole operator | MEDIUM — API_CATALOG.md rows for these endpoints rely on doc-sourced evidence (MEDIUM confidence) rather than independently re-read source; a future DELTA pass should close this |
| `signals/engine.py` internals | Exact `run_signal_engine`/verdict-threshold/sector-weight logic | Only grepped for module boundaries, not fully read by any dispatched agent this engagement (docs/architecture.md's detailed description was used as MEDIUM-confidence secondary evidence) | Sole operator | LOW — the persisted `verdict_history.recommendation` value comes from the *analyst's* validated output, not the signal engine's own verdict field directly, per the business-flow agent's confirmed reading of `verdict_history.save_snapshot()`'s call sites |
| `main._build_report()` internals | Exact mechanics of how the `degraded` flag is promoted from `analyst/crew.py`'s internal `_degraded` marker onto the `Report` object | Not independently read line-by-line by any dispatched agent — corroborated only by `docs/architecture.md`'s prose description | Sole operator | LOW |
| `corporate_actions_pipeline.py` health gate | Does this pipeline have an equivalent `>50%`-error-rate (or similar) health gate to its four sibling pipelines, or is `docs/backlog.md`'s disclosed "near-total silent-failure blind spot" still current? | The routes/pipelines research agent read this file partially and explicitly noted "no `_MAX_ACCEPTABLE_ERROR_RATE` found in this file" but did not confirm this as a complete negative (file not fully read) | Sole operator | MEDIUM — feeds Top Smell #10 in `RISK_MAP.md`; recommend a targeted follow-up read of the full file |
| API-key rate-limit ordering | Does `_require_api_key_user()` on `GET /api/v1/consolidated/{symbol}` now call `_rate_limit()` before the DB lookup, closing the gap `docs/api-reference.md`/`docs/backlog.md` describe as fixed? | No dispatched agent independently re-read this specific code path this engagement (P3b's control 5 addressed *general* rate-limiting design, not this specific endpoint's fix status) | Sole operator | MEDIUM — `RISK_MAP.md` Top Smell #8 carries this at MEDIUM confidence pending verification |
| Broker-sync live verification | Have HDFC Securities' and Paytm Money's REST shapes (base URL, endpoint paths, checksum-signing scheme, response field names) been verified against a real live account sync, or are they still inferred from public docs/SDK snippets? | `docs/backlog.md`'s own "Untested, closes on first real use" section discloses this is still unverified for HDFC/Paytm (Zerodha's `kiteconnect` integration does not carry this caveat); this engagement's agents read the code but performed no live network calls (correctly, per read-only boundary) | Sole operator | MEDIUM — carried into `API_CATALOG.md` rows #58,62,63 and `PRD.md` as a disclosed implementation-status caveat, not a comprehension gap |
| CAS/CSV import real-file verification | Has a real CAMS/KFintech CAS PDF or a real broker CSV export been run through `cas_import.py`/`csv_import.py`, beyond synthetic test fixtures? | `docs/backlog.md`'s own "Untested, closes on first real use" section discloses this is unverified; this engagement performed no live file uploads (correctly, per read-only boundary) | Sole operator | LOW — code-level PII scrubbing, idempotency, and dedup behavior were independently verified against source by the P3b and routes/pipelines agents regardless of real-file testing status |
| yfinance `sector` taxonomy | Does yfinance's `sector` field for NSE/BSE symbols actually match the GICS-like taxonomy `signals/engine.py`'s sector-weight tilts assume? | `backend/CLAUDE.md` discloses this was never verified against a live response in this sandbox; the quality/ops agent additionally found a real in-repo contradiction — test fixtures use a different taxonomy (`"IT"`/`"Banking"`) than the sector-tilt code apparently expects | Sole operator | LOW-MEDIUM — affects signal-engine calibration confidence, not domain-comprehension confidence; already tracked in `docs/backlog.md`'s "Signal engine & data quality" section |
| Deployment topology detail | `docker-compose.yml`'s exact service definitions, healthchecks, and the disclosed direct-port-mapping risk's current status | Not independently re-opened by any dispatched agent this engagement — `DEPENDENCY_GRAPH.md`'s deployment view is doc-sourced only (MEDIUM) | Sole operator | LOW — does not affect the domain model, only operational detail already tracked in `docs/backlog.md`'s "Deployment & operations" section |
| Runtime behavior (all contexts) | No runtime telemetry exists to confirm any flow's *actual* production traffic pattern, error rate, or latency | P2b (Datadog/KubeSense) is skipped this engagement — see `KNOWN_OMISSIONS.md` | N/A | Caps every flow/dependency claim in this engagement at code-derived confidence (max HIGH via static evidence); no `runtime_confirmed` claims exist anywhere in these deliverables |
| Cross-domain SQL joins | Whether any query joins tables owned by two different bounded contexts in one statement (the "Cross-domain join" smell) | No dispatched agent was specifically tasked with a repo-wide join-audit across context boundaries; none was incidentally surfaced | Sole operator | LOW — recorded as UNKNOWN rather than asserted "none found," per this engagement's evidence rules |
